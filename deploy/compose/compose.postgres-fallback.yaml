services:
  migrate:
    environment:
      GRAPH_BACKEND: postgres
      GRAPH_FALLBACK_ENABLED: "true"
  worker:
    environment:
      GRAPH_BACKEND: postgres
      GRAPH_FALLBACK_ENABLED: "true"
      WORKER_BATCH_SIZE: 100
